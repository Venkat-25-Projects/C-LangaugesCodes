#include<stdio.h>
#include<math.h>
int main()
{
	
	//These are the some math functions which are used to perform the operations.
	
	
	double A = sqrt(9);              //Square root
	double B = pow(2,4);            //power of
	int C = round(3.14);           // Rounding the value Exact like 3
	int D = ceil(3.14);           //Rounding the value raise the value upto 4
	int E = floor(3.99);         //Rounding the value down
	double F = fabs(-100);      //Absolute function of the value negative to positive
	double G = log(3);         //Logthirithm of the numbers 
	double H = sin(45);       // These all are the triganomatrey functions 
	double I = cos(45);
	double J = tan(45);
	
	printf("\n%lf",A);
	printf("\n%lf",B);
	printf("\n%lf",C);
	printf("\n%lf",D);
	printf("\n%lf",E);
	printf("\n%lf",F);
	printf("\n%lf",G);
	printf("\n%lf",H);
	printf("\n%lf",I);
	printf("\n%lf",J);
	
	
}
